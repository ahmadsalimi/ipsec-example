version: "3.9"

services:
  server1:
    image: ubuntu
    networks:
      net1:
        ipv4_address: 10.5.1.2
    hostname: server1
    command:
      - watch
      - ls

  server2:
    image: ubuntu
    networks:
      net2:
        ipv4_address: 10.5.2.2
    hostname: server2
    command:
      - watch
      - ls

  ipsec1:
    build: ./openswan
    networks:
      ipsec:
        ipv4_address: 10.5.3.2
      net1:
        ipv4_address: 10.5.1.3
      net2:
        ipv4_address: 10.5.2.3
    hostname: ipsec1
    volumes:
      - ./ipsec1.conf:/etc/ipsec.conf
    command:
      - watch
      - ls

  ipsec2:
    build: ./openswan
    networks:
      ipsec:
        ipv4_address: 10.5.3.3
      net1:
        ipv4_address: 10.5.1.4
      net2:
        ipv4_address: 10.5.2.4
    hostname: ipsec2
    volumes:
      - ./ipsec2.conf:/etc/ipsec.conf
    command:
      - watch
      - ls

networks:
  net1:
    driver: bridge
    ipam:
      config:
        - subnet: 10.5.1.0/24
          gateway: 10.5.1.1

  net2:
    driver: bridge
    ipam:
      config:
        - subnet: 10.5.2.0/24
          gateway: 10.5.2.1

  ipsec:
    driver: bridge
    ipam:
      config:
        - subnet: 10.5.3.0/24
          gateway: 10.5.3.1
